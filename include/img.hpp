#pragma once

#include <string>

namespace fdt {

    namespace img {

        void bboxCrop(const std::string &, const std::string &,
                      const std::string &);

    } // namespace img

} // namespace fdt
